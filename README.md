# photo-management
A cloud-based photo management service with AI-powered analysis and semantic search

# CLI commands
```bash
python -m app.cli create-schema
python -m app.cli create-basedata
```

# Run the project using uvicorn
```bash
uvicorn app.main:app --reload
```